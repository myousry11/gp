import 'package:flutter/material.dart';

class PlantsTasksScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Done tasks',
        style: TextStyle(
          fontWeight: FontWeight.bold,
          fontSize: 30.0
        ),
      ),
    );
  }
}
